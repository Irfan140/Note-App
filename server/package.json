{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.js",
    "db:migrate": "cd prisma && bunx prisma migrate dev --name init"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^6.18.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@clerk/express": "^1.7.44",
    "@types/express": "^5.0.5",
    "express": "^5.1.0"
  }
}
